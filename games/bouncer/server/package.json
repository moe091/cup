{
  "name": "@cup/bouncer-server",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 755350934435.dkr.ecr.us-east-1.amazonaws.com && docker build -f Dockerfile -t cup-bouncer:local ../../.. && docker tag cup-bouncer:local 755350934435.dkr.ecr.us-east-1.amazonaws.com/cup/bouncer-server:latest && docker push 755350934435.dkr.ecr.us-east-1.amazonaws.com/cup/bouncer-server:latest",
    "build:watch": "tsc -p tsconfig.json -w",
    "start:watch": "node --watch dist/main.js",
    "start:dev": "pnpm run build:watch & pnpm run start:watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm run typecheck",
    "format": "prettier --config ../../../apps/api/.prettierrc --check \"src/**/*.ts\"",
    "format:fix": "prettier --config ../../../apps/api/.prettierrc --write \"src/**/*.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.26.1",
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.0.3",
    "prettier": "^3.4.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@cup/bouncer-engine": "workspace:*",
    "@cup/bouncer-shared": "workspace:*",
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.3",
    "socket.io": "^4.8.3"
  }
}
